.topic_panel{data: {topic_id: topic.id}}
  %h2.title
    = topic.title
  - unless topic.alternate_titles.empty?
    %p.alternate_titles
      a.k.a.
      = topic.alternate_titles.map(&:title).join(', ')
  %p.description
    = topic.description
  %ul
    - topic.resources.each do |resource|
      %li.resource
        = link_to resource.display_title, resource.url
