# LearnWeb (working title)

## Getting Started

Requires [Node.js](http://nodejs.org/).

```bash
bundle install
rake db:migrate
npm install -g bower
bower install

rails server
open http://localhost:3000
```
